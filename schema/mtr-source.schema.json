{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/mtr-source.schema.json",
  "title": "MTR editable YAML source",
  "oneOf": [
    { "$ref": "#/$defs/manifest" },
    { "$ref": "#/$defs/chapterFile" }
  ],
  "$defs": {
    "localizedText": {
      "type": "object",
      "additionalProperties": false,
      "required": ["en", "zh"],
      "properties": {
        "en": { "type": "string", "minLength": 1 },
        "zh": { "type": "string", "minLength": 1 }
      }
    },
    "manifest": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schemaVersion", "document", "files"],
      "properties": {
        "schemaVersion": { "const": 1 },
        "document": {
          "type": "object",
          "additionalProperties": false,
          "required": ["id", "title", "version", "effectiveDate"],
          "properties": {
            "id": { "const": "mtr" },
            "title": { "$ref": "#/$defs/localizedText" },
            "version": { "type": "string", "pattern": "^[0-9]{8}$" },
            "effectiveDate": { "type": "string", "format": "date" }
          }
        },
        "files": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(?:introduction|chapter-(?:0[1-9]|10)|appendix-[a-f])\\.yaml$"
          }
        }
      }
    },
    "chapterFile": {
      "type": "object",
      "additionalProperties": false,
      "required": ["chapter"],
      "properties": {
        "chapter": { "$ref": "#/$defs/chapter" }
      }
    },
    "chapter": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "chapter", "en", "zh"],
      "properties": {
        "id": { "type": "string", "pattern": "^mtr-[a-z0-9.-]+$" },
        "chapter": { "type": "string", "minLength": 1 },
        "en": { "type": "string", "minLength": 1 },
        "zh": { "type": "string", "minLength": 1 },
        "groups": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/group" }
        },
        "sections": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/section" }
        }
      },
      "anyOf": [
        { "required": ["groups"] },
        { "required": ["sections"] }
      ]
    },
    "section": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "chapter", "en", "zh", "groups"],
      "properties": {
        "id": { "type": "string", "pattern": "^mtr-[a-z0-9.-]+$" },
        "chapter": { "type": "string", "minLength": 1 },
        "en": { "type": "string", "minLength": 1 },
        "zh": { "type": "string", "minLength": 1 },
        "groups": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/group" }
        }
      }
    },
    "group": {
      "oneOf": [
        { "$ref": "#/$defs/textGroup" },
        { "$ref": "#/$defs/imageGroup" }
      ]
    },
    "textGroup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "blocks"],
      "properties": {
        "id": { "type": "string", "pattern": "^mtr-[a-z0-9.-]+-g[0-9]{3,}$" },
        "type": { "enum": ["paragraph", "list", "table", "markdown"] },
        "blocks": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/textBlock" }
        }
      }
    },
    "imageGroup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "blocks"],
      "properties": {
        "id": { "type": "string", "pattern": "^mtr-[a-z0-9.-]+-g[0-9]{3,}$" },
        "type": { "const": "image" },
        "blocks": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/imageBlock" }
        }
      }
    },
    "textBlock": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "en", "zh"],
      "properties": {
        "id": { "type": "string", "pattern": "^mtr-[a-z0-9.-]+-b[0-9]{3,}$" },
        "en": { "type": "string", "minLength": 1 },
        "zh": { "type": "string" },
        "extras": {
          "type": "array",
          "maxItems": 1,
          "items": { "$ref": "#/$defs/extra" }
        },
        "joinAfter": { "$ref": "#/$defs/joinAfter" }
      }
    },
    "imageBlock": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "asset", "alt"],
      "properties": {
        "id": { "type": "string", "pattern": "^mtr-[a-z0-9.-]+-b[0-9]{3,}$" },
        "asset": {
          "type": "string",
          "pattern": "^assets/[a-zA-Z0-9._/-]+\\.png$"
        },
        "alt": { "$ref": "#/$defs/localizedText" },
        "extras": {
          "type": "array",
          "maxItems": 1,
          "items": { "$ref": "#/$defs/extra" }
        }
      }
    },
    "extra": {
      "type": "object",
      "additionalProperties": false,
      "required": ["en", "zh"],
      "properties": {
        "en": { "type": "string", "minLength": 1 },
        "zh": { "type": "string" }
      }
    },
    "joinAfter": {
      "type": "object",
      "additionalProperties": false,
      "required": ["en", "zh"],
      "properties": {
        "en": { "type": "string" },
        "zh": { "type": "string" }
      }
    }
  }
}

